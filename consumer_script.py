#this is a mock script that emulates a file that would use calculation_module.py

import calculation_module

required_output = calculation_module.add(15,23)
print("I needed the result of 15+23 and i got:", required_output)
